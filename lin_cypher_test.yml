---
- name: Cypher Test
  hosts: all
  become: True
  pre_tasks:      
    - name: include morpheus_vars.yml
      include_vars: ./vars/morpheus_vars.yml
  tasks:
  - name: Print out cypher
    debug: 
      msg: "This is a secret from Cypher: {{ lookup('cypher','secret=password/test') }}"